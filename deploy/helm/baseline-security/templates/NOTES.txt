baseline-security {{ .Chart.AppVersion }} installed.

The operator scans this cluster against: {{ join ", " .Values.profiles }}.
Open the console under Administration -> Compliance to see the score.

Notes:
- The Compliance Operator is a prerequisite/shared component; this chart does not
  install or remove it. Ensure it is available (installComplianceOperator on the
  ClusterBaseline can create its Subscription).
- To skip the default ClusterBaseline, set defaultClusterBaseline=false.
