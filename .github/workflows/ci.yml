name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  operator:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: operator
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version-file: operator/go.mod
          cache-dependency-path: operator/go.sum
      - run: make test
      - run: make lint
      - name: verify generated files are current
        run: |
          make generate manifests
          git diff --exit-code
      - run: make verify-csv-rbac

  console-plugin:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: console-plugin
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: npm i -g corepack && corepack enable
      - run: yarn install --immutable
      - run: yarn lint
      - run: yarn test
      - run: yarn build

  images:
    runs-on: ubuntu-latest
    needs: [operator, console-plugin]
    steps:
      - uses: actions/checkout@v7
      - run: docker build -t baseline-security-operator operator/
      - run: docker build -t baseline-security-console-plugin console-plugin/
