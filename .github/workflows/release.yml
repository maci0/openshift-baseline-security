name: release

# Publish the operator, console-plugin, OLM bundle, and catalog images to
# quay.io on a version tag. The CSV/Makefile already reference
# quay.io/openshift-baseline-security/*, so a tag push is all it takes to make
# an OLM-installable release the community can pull. amd64 only for now
# (OpenShift's primary arch); add QEMU + buildx platforms when arm64 is needed.
on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Cut version matching Makefile VERSION and git tag vX.Y.Z (not a re-tag of a published cut)'
        required: true

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      REGISTRY: quay.io/openshift-baseline-security
      # Classic docker driver (build then push, image lands in the local store).
      # Drop the buildx-only --provenance/--sbom from the Makefile default; the
      # release does not need reproducible-digest parity with the CI images job.
      # ?= in the Makefile defers to this exported value.
      DOCKER_BUILD_FLAGS: --pull --build-arg SOURCE_DATE_EPOCH=0
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-tags: true

      # Version from the tag (strip leading v) or the manual input; must match
      # the Makefile VERSION so a release never ships images tagged differently
      # from the CSV's own image references. Refuse a cut whose git tag is
      # missing, a different commit than HEAD, or whose CHANGELOG still has
      # Unreleased bullets (would publish unreleased work as :VERSION).
      - name: resolve and verify version
        # Input passed via env, never shell-interpolated: ${{ }} expansion into
        # run: would let a crafted workflow_dispatch version execute commands.
        env:
          INPUT_VERSION: ${{ github.event.inputs.version }}
        run: |
          set -euo pipefail
          if [ -n "$INPUT_VERSION" ]; then
            ver="$INPUT_VERSION"
          else
            ver="${GITHUB_REF_NAME#v}"
          fi
          mk=$(sed -n 's/^VERSION ?= //p' operator/Makefile | head -1)
          if [ "$ver" != "$mk" ]; then
            echo "tag/input version ($ver) != operator/Makefile VERSION ($mk)" >&2
            exit 1
          fi
          if ! git rev-parse -q --verify "refs/tags/v$ver" >/dev/null; then
            echo "git tag v$ver missing; tag the cut before publishing" >&2
            exit 1
          fi
          tagged=$(git rev-parse "refs/tags/v$ver^{commit}")
          head=$(git rev-parse HEAD)
          if [ "$tagged" != "$head" ]; then
            echo "refusing to publish $ver from $head; v$ver points at $tagged" >&2
            exit 1
          fi
          make -C operator RELEASE_GATE=1 REQUIRE_GIT_TAGS=1 verify-versions
          echo "VERSION=$ver" >> "$GITHUB_ENV"

      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_TOKEN }}

      # push_tags builds VERSION then retags + pushes :latest as a convenience
      # pointer. :VERSION is the immutable one the CSV pins.
      - name: operator image
        run: |
          set -euo pipefail
          make -C operator docker-build docker-push IMG="$REGISTRY/baseline-security-operator:$VERSION"
          docker tag "$REGISTRY/baseline-security-operator:$VERSION" "$REGISTRY/baseline-security-operator:latest"
          docker push "$REGISTRY/baseline-security-operator:latest"

      - name: console-plugin image
        run: |
          set -euo pipefail
          img="$REGISTRY/baseline-security-console-plugin:$VERSION"
          docker build $DOCKER_BUILD_FLAGS -f console-plugin/Dockerfile -t "$img" console-plugin
          docker push "$img"
          docker tag "$img" "$REGISTRY/baseline-security-console-plugin:latest"
          docker push "$REGISTRY/baseline-security-console-plugin:latest"

      - name: bundle image
        run: |
          set -euo pipefail
          make -C operator bundle-build bundle-push BUNDLE_IMG="$REGISTRY/baseline-security-operator-bundle:$VERSION"
          docker tag "$REGISTRY/baseline-security-operator-bundle:$VERSION" "$REGISTRY/baseline-security-operator-bundle:latest"
          docker push "$REGISTRY/baseline-security-operator-bundle:latest"

      # Catalog references the bundle pullspec, so build it after the bundle push.
      - name: catalog image
        run: |
          set -euo pipefail
          make -C operator catalog-build catalog-push \
            BUNDLE_IMG="$REGISTRY/baseline-security-operator-bundle:$VERSION" \
            CATALOG_IMG="$REGISTRY/baseline-security-operator-catalog:$VERSION"
          docker tag "$REGISTRY/baseline-security-operator-catalog:$VERSION" "$REGISTRY/baseline-security-operator-catalog:latest"
          docker push "$REGISTRY/baseline-security-operator-catalog:latest"

  # CycloneDX SBOM for every pushed image, attached to the GitHub release so
  # consumers and vuln scanners have an inventory of what shipped. Kept out of
  # the publish job so image digests stay untouched (matches ci.yml's
  # --provenance=false --sbom=false reproducibility stance).
  sbom:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    needs: [publish]
    # contents:write is only for attaching SBOM files to the existing release;
    # checkout runs with persist-credentials:false so no token lands on disk.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: resolve and verify version
        # Same env indirection as the publish job: no raw input in shell source.
        env:
          INPUT_VERSION: ${{ github.event.inputs.version }}
        run: |
          set -euo pipefail
          if [ -n "$INPUT_VERSION" ]; then
            ver="$INPUT_VERSION"
          else
            ver="${GITHUB_REF_NAME#v}"
          fi
          mk=$(sed -n 's/^VERSION ?= //p' operator/Makefile | head -1)
          if [ "$ver" != "$mk" ]; then
            echo "tag/input version ($ver) != operator/Makefile VERSION ($mk)" >&2
            exit 1
          fi
          echo "VERSION=$ver" >> "$GITHUB_ENV"
          echo "TAG=v$ver" >> "$GITHUB_ENV"

      - name: SBOM operator image
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: quay.io/openshift-baseline-security/baseline-security-operator:${{ env.VERSION }}
          format: cyclonedx-json
          artifact-name: baseline-security-operator-${{ env.VERSION }}-sbom.cdx.json
          upload-artifact: true
          upload-release-assets: false

      - name: SBOM console-plugin image
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: quay.io/openshift-baseline-security/baseline-security-console-plugin:${{ env.VERSION }}
          format: cyclonedx-json
          artifact-name: baseline-security-console-plugin-${{ env.VERSION }}-sbom.cdx.json
          upload-artifact: true
          upload-release-assets: false

      - name: SBOM bundle image
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: quay.io/openshift-baseline-security/baseline-security-operator-bundle:${{ env.VERSION }}
          format: cyclonedx-json
          artifact-name: baseline-security-operator-bundle-${{ env.VERSION }}-sbom.cdx.json
          upload-artifact: true
          upload-release-assets: false

      - name: SBOM catalog image
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: quay.io/openshift-baseline-security/baseline-security-operator-catalog:${{ env.VERSION }}
          format: cyclonedx-json
          artifact-name: baseline-security-operator-catalog-${{ env.VERSION }}-sbom.cdx.json
          upload-artifact: true
          upload-release-assets: false

      # Attach to the release when one exists (tag push); a dispatch from that
      # tagged commit without a GitHub release keeps SBOMs as workflow artifacts
      # instead of failing the job.
      - name: attach SBOMs to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if gh release view "$TAG" --json id >/dev/null 2>&1; then
            gh release upload "$TAG" \
              "baseline-security-operator-$VERSION-sbom.cdx.json" \
              "baseline-security-console-plugin-$VERSION-sbom.cdx.json" \
              "baseline-security-operator-bundle-$VERSION-sbom.cdx.json" \
              "baseline-security-operator-catalog-$VERSION-sbom.cdx.json" \
              --clobber
          else
            echo "no GitHub release for $TAG; SBOMs remain as workflow artifacts" >&2
          fi
