apiVersion: v1
kind: Service
metadata:
  name: baseline-security-operator-metrics
  namespace: openshift-baseline-security
  labels:
    app: baseline-security-operator
    control-plane: controller-manager
spec:
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: 8443
  selector:
    app: baseline-security-operator
