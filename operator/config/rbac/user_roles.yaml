# Human-facing roles, aggregated into the built-in view/admin roles so
# console users get sensible access without extra bindings.
# Viewer lists every kind the console watches (including TailoredProfiles
# and Rules on the Profiles tab). Admin adds the writes: ClusterBaseline
# patch, scan rescan, remediation apply, TailoredProfile create/update.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: baseline-security-viewer
  labels:
    rbac.authorization.k8s.io/aggregate-to-view: "true"
    rbac.authorization.k8s.io/aggregate-to-cluster-reader: "true"
rules:
  - apiGroups: [baselinesecurity.openshift.io]
    resources: [clusterbaselines]
    verbs: [get, list, watch]
  - apiGroups: [compliance.openshift.io]
    resources:
      - compliancecheckresults
      - compliancescans
      - compliancesuites
      - complianceremediations
      - profiles
      - tailoredprofiles
      - rules
    verbs: [get, list, watch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: baseline-security-admin
  labels:
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
rules:
  - apiGroups: [baselinesecurity.openshift.io]
    resources: [clusterbaselines]
    verbs: [get, list, watch, update, patch]
  - apiGroups: [compliance.openshift.io]
    resources: [compliancescans]
    verbs: [get, list, watch, patch]
  - apiGroups: [compliance.openshift.io]
    resources: [complianceremediations]
    verbs: [get, list, watch, update, patch]
  - apiGroups: [compliance.openshift.io]
    resources: [tailoredprofiles]
    verbs: [get, list, watch, create, update, patch]
